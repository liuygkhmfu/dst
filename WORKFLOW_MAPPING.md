# 工作流迁移映射

来源文件：`0722稳定版本 (2).json`。原型共 47 个节点、45 组连接。Python 版本将重复的“准备请求 → HTTP → Base64 解析 → 保存”合并到公共服务函数。

| Python 任务 | 原型来源 | 提示词组 | 参考图 |
|---|---|---|---|
| 03～06 | `氛围感摆放1` → `JavaScript1` → `分流-图03至图06` | placement | `stt` / `product_image` |
| 07～12 | `场景图1` → `JavaScript2` → `分流-图07至图12` | usage_scene | 默认 `stt` |
| 08 | `GPT拟真14` → `GPT拟真15` → `GPT教具拟真结果7` | usage_scene | `cpt + stt`，其中 cpt 在无上传时自动跳过 |
| 13 | `尺寸图` → `整理尺寸图承接节点` → `GPT拟真18` | size | `stt` |

## 已知原型问题

1. `分流-图07至图12` 的图08输出端口为空，但悬空链路 `GPT拟真14 → GPT拟真15 → GPT教具拟真结果7` 看起来就是图08。Python 版本将它保留为配置驱动的“图08（断线链路）”，用户可在 `config/task_definitions.py` 中停用或调整参考图。
2. `尺寸图` 的提示词内部要求“图1：白底图”和“图2：尺寸图”两个槽位，但承接逻辑最终只有一个 `task_id=13`。当前任务名称明确标为“白底图 / 尺寸图（待确认）”，未擅自拆分。
3. 原型共有 03、04、05、06、07、08、09、10、11、12、13 共 11 个实际任务；图01、图02没有稳定的最终承接任务，Python MVP 不自行补齐。

## 新增额外参考图需求

新建项目页的每条额外需求都生成一个 `XR-01`、`XR-02`…任务：

```text
产品身份图 → 本条额外参考图（可多张） → 本条文字要求 → 单独图片任务
```

这些任务和批量工作流任务共享版本审核、重新生成、局部编辑、最终确认和导出逻辑，但不同需求之间不会互相携带参考图。

